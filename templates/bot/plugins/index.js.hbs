/*
 * @author wingbot.ai
 */
'use strict';

const { Plugins } = require('wingbot');

const plugins = new Plugins();

// register plugins here, but its good to keep them in separate files
//
// plugins.register('pluginName', (req, res, postBack, context, paramsData) => {
//
// });

module.exports = plugins;
